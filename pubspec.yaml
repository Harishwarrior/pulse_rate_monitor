name: pulse_rate_monitor
description: Measure PPG using the phone's camera.

version: 1.3.0+4

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  charts_flutter: ^0.12.0
  flutter:
    sdk: flutter
  flutter_better_camera: ^1.0.3
  flutter_riverpod: ^2.3.6
  percent_indicator: ^4.2.3
  riverpod_annotation: ^2.1.1
  shared_preferences: ^2.1.0
  wakelock: ^0.6.2

dev_dependencies:
  build_runner: ^2.3.3
  flutter_test:
    sdk: flutter
  riverpod_generator: ">=2.2.1 <4.0.0"
  riverpod_lint: ">=1.3.1 <3.0.0"
  very_good_analysis: ">=4.0.0+1 <9.0.0"

flutter:
  uses-material-design: true
